import * as React from "react";
export default class Layout extends React.Component<any, any> {
    constructor(props: any);
    renderSectionHeader(title: string): JSX.Element;
    renderItem(title: string, info: string): JSX.Element;
    render(): JSX.Element;
}
