import * as React from "react";
declare type State = {
    code: string;
    errors: string[];
};
export default class Download extends React.Component<any, State> {
    render(): JSX.Element;
}
export {};
